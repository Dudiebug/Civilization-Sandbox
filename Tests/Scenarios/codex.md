# Scenario Test Scope

Use versioned seeds, explicit initial state, command log, expected checkpoints, invariants, and traces. Every consequential behavior needs positive, negative, and no-valid-target cases.
