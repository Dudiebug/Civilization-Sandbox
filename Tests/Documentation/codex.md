# Documentation Validation Test Scope

Use temporary synthetic repositories for negative cases. Never damage a tracked source document to prove validation. Tests must remain offline, deterministic, standard-library-only, and assert actionable diagnostic codes.
