# CI Workflow Scope

CI must use repository commands, retain evidence, and fail closed. Never make a required deterministic, migration, architecture, or performance gate advisory without an approved ADR. Pin action revisions. Do not expose secrets to untrusted code or enable unrestricted network access for routine validation.
