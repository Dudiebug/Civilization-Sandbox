# Configuration Scope

Configuration files are versioned control assets. The task registry is milestone-aware: the first 25 tasks cover Milestones 0.1-0.3, while later task contracts are created only after the preceding gate passes. Do not assign future task IDs or silently move tasks between milestones without updating the master plan, status tooling, and an accepted planning decision.
