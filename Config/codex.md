# Configuration Scope

Configuration and registries are versioned inputs. Use stable IDs, schemas, explicit units, deterministic ordering, and validation. Do not store secrets, machine-local absolute paths, mutable runtime state, or unreviewed future-era content.
