# Engine Project Settings Scope

This folder is provisional until Milestone 0.1 accepts an engine/toolchain path. If Unity is selected, project settings are versioned architecture. Changes require an explicit reason, before/after evidence, platform impact, and clean-checkout validation. Do not use settings to mask simulation or performance defects. Preserve deterministic/reference-build assumptions and accessibility/input extensibility.
