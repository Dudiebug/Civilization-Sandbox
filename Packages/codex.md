# Engine Package Configuration Scope

This folder is provisional until Milestone 0.1 accepts an engine/toolchain path. If Unity is selected, pin exact package versions and commit the lock. No package upgrade is incidental. An upgrade requires a task or ADR, clean build/tests, determinism/replay check, save impact review, performance comparison, and rollback instructions.
