# UI and Accessibility Scope

UI reads immutable query models and sends validated commands. It never edits ECS/domain state directly. Provide plain-language cause first, deeper evidence on demand, full keyboard remapping, scalable text/UI, non-color encodings, reduced motion/effects, pause-and-inspect, and bounded information density.
